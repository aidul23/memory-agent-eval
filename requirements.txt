# Core
pyyaml>=6.0.1
python-dotenv>=1.0.1
pydantic>=2.6.0
typing-extensions>=4.10.0

# CLI
click>=8.1.7

# Data / Analysis
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
statsmodels>=0.14.1
matplotlib>=3.8.0
seaborn>=0.13.0

# LLM providers (all optional at runtime; the platform falls back to a Mock provider)
openai>=1.30.0
anthropic>=0.30.0
google-generativeai>=0.7.0
requests>=2.31.0

# External memory providers (optional - install only the ones you actually run).
# Each wrapper falls back to a clearly-logged local stub if the SDK is missing,
# so the platform stays runnable without these.
# mem0ai>=0.1.94          # self-hostable in-process library; needs a vector store
# qdrant-client>=1.9.0    # Mem0 default vector store backend
# zep-cloud>=2.0.0        # works against both Zep Cloud and self-hosted CE (via base_url)
# acontext>=0.2.0         # AContext Python client
# hindsight-client>=0.6.0 # Vectorize.io Hindsight client

# Optional dashboard
streamlit>=1.33.0

# Tests
pytest>=8.0.0
